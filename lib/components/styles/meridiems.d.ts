export declare const meridiemWrapper: import("@emotion/utils").SerializedStyles;
interface Props {
    isRight?: boolean;
    isSelected: boolean;
}
export declare const meridiem: ({ isRight, isSelected }: Props) => import("@emotion/utils").SerializedStyles;
export {};
